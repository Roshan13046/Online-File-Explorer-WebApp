//learning path modules

const path = require('path');
console.log(path.join('Node','Project','namastey Codechef'));

console.log(path.normalize('../../Node/../Built in Module fs'));

console.log(path.resolve('../../Node/../Built in Modues fs'));

//we are installing nodemon npm to prevent frequent restarting of the app manually after making some changes.